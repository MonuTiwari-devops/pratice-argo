apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-argo-demo.fullname" . }}
  labels:
    {{- include "my-argo-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-argo-demo.selectorLabels" . | nindent 4 }}
