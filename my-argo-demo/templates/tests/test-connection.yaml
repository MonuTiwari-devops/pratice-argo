apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-argo-demo.fullname" . }}-test-connection"
  labels:
    {{- include "my-argo-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-argo-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
